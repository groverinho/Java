
public class amigos {

	public static void main(String[] args) {
		/*
		 #include <iostream>
#include <string>
#include <cstdio>
using namespace std;

int main()
{
    int contestants; scanf("%d", &contestants);
    string record[contestants];
    for(int i = 0; i < contestants; i++)
        cin >> record[i];

    int multi[contestants][contestants];
    for(int i = 0; i < contestants; i++)
        for(int j = 0; j < contestants; j++)
            scanf("%d", &multi[i][j]);

    int queries; scanf("%d", &queries);
    while(queries--)
    {
        string aux1, aux2; cin >> aux1 >> aux2;
        int pos1 = -1, pos2 = -1;

        for(int i = 0; i < contestants; i++)
        {
            if(aux1 == record[i])
                pos1 = i;
            if(aux2 == record[i])
                pos2 = i;
            if(pos1 > -1 && pos2 > -1)
                break;
        }

        printf("%d\n", multi[pos1][pos2]);
    }
    return 0;
}
		 * */

	}

}
